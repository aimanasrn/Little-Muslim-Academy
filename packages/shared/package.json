{
  "name": "@little-muslim/shared",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node -e \"const fs = require('fs'); const path = require('path'); const pkgDir = path.dirname(process.env.npm_package_json); fs.rmSync(path.join(pkgDir, 'dist'), { recursive: true, force: true });\" && tsc -p tsconfig.json --ignoreDeprecations 6.0 --rootDir src"
  },
  "devDependencies": {
    "typescript": "latest"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  }
}
